apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- cmp-plugin.yaml

patches:
- path: argocd-repo-server.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: argocd-repo-server