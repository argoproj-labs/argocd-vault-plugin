name: Basic pipeline
on: push

jobs:
  CI:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Format code
        run: make format

      - name: Quality checks
        run: make quality

      - name: Build and test plugin
        run: make e2e