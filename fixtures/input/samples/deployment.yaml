apiVersion: apps/v1
kind: Deployment
metadata:
  name: name
  namespace: namespace
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:<tag>
        ports:
        - containerPort: 80
