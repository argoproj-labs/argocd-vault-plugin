apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: <name>
spec:
  schedule: "30 21 * * 1-5"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: <name>
            image: <name>:<tag> 