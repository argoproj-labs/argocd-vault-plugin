apiVersion: batch/v1
kind: Job
metadata:
  name: <name>
spec:
  template:
    spec:
      containers:
      - name: <name>
        image: <name>:<tag>
        envFrom:
        - configMapRef:
            name: a-configmap